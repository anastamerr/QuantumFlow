# Server
HOST=0.0.0.0
PORT=8000

# Comma-separated list. Use "*" for all.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Qiskit backend name. Defaults to Aer simulator
QISKIT_BACKEND=aer_simulator

# Maximum number of qubits allowed for statevector simulation (response includes full statevector)
STATEVECTOR_MAX_QUBITS=10
